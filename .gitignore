__pycache__/
.venv
.env
requirements.txt
hostbot.log
notes.txt

# Per-environment settings, copied over .env to switch (see CLAUDE.md)
.env.dev
.env.stage
.env.prod

# Backups of the above. Same secrets, so the same rule.
.env*.bak

# Agent instructions, kept local
CLAUDE.md
