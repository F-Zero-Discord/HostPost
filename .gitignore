__pycache__/
.venv
.env
requirements.txt
hostbot.log
notes.txt

# Per-environment settings, read with `hostpost --env NAME` (see AGENTS.md)
.env*
!.env.example

# Backups of the above. Same secrets, so the same rule.
.env*.bak

# Agent instructions, kept local
CLAUDE.md
